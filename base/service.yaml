apiVersion: v1
kind: Service
metadata:
  name: eddie-register-app-service
spec:
  type: LoadBalancer
  selector:
    app: eddie-register-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080